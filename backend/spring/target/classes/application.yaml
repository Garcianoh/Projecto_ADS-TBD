spring:
  application:
    name: boleia

  datasource:
    url: jdbc:oracle:thin:@oracle-db:1521/FREEPDB1
    username: system
    password: oracle
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: troca-esta-chave-por-uma-bem-grande-e-aleatoria-depois
  expiration: 86400000   # 24 horas em milissegundos

server:
  port: 8080